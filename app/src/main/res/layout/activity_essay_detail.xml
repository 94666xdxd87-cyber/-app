<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:background="#FAFAFA">

    <!-- 標題列 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="16dp"
        android:background="#FFFFFF"
        android:elevation="3dp">

        <ImageButton
            android:id="@+id/btnDetailBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_media_previous"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="返回"/>

        <TextView
            android:id="@+id/tvDetailSubject"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="科目"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:paddingStart="8dp"/>

        <TextView
            android:id="@+id/tvDetailTimestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#9E9E9E"/>
    </LinearLayout>

    <!-- 內容捲動區 -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="@dimen/bottom_safe_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- 題目卡片 -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📌 題目"
                android:textSize="13sp"
                android:textColor="#616161"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"/>

            <TextView
                android:id="@+id/tvDetailQuestion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="15sp"
                android:textColor="#212121"
                android:lineSpacingMultiplier="1.4"
                android:padding="14dp"
                android:background="#FFFFFF"
                android:elevation="1dp"
                android:layout_marginBottom="16dp"/>

            <!-- 學生作答卡片 -->
            <LinearLayout
                android:id="@+id/cardDetailUserAnswer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginBottom="16dp">

                <TextView
                    android:id="@+id/tvDetailUserAnswerLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="✏️ 我的回答"
                    android:textSize="13sp"
                    android:textColor="#616161"
                    android:textStyle="bold"
                    android:layout_marginBottom="6dp"/>

                <TextView
                    android:id="@+id/tvDetailUserAnswer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="14sp"
                    android:textColor="#212121"
                    android:lineSpacingMultiplier="1.4"
                    android:padding="14dp"
                    android:background="#FFFFFF"
                    android:elevation="1dp"/>
            </LinearLayout>

            <!-- AI 回覆卡片 -->
            <LinearLayout
                android:id="@+id/cardDetailAiReply"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginBottom="16dp">

                <TextView
                    android:id="@+id/tvDetailAiReplyLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🤖 AI 評分回覆"
                    android:textSize="13sp"
                    android:textColor="#616161"
                    android:textStyle="bold"
                    android:layout_marginBottom="6dp"/>

                <TextView
                    android:id="@+id/tvDetailAiReply"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="14sp"
                    android:textColor="#1A237E"
                    android:lineSpacingMultiplier="1.5"
                    android:padding="14dp"
                    android:background="#EEF2FF"
                    android:elevation="1dp"/>
            </LinearLayout>

            <!-- 操作按鈕列 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="10dp">

                <Button
                    android:id="@+id/btnDetailEdit"
                    android:layout_width="0dp"
                    android:layout_height="48dp"
                    android:layout_weight="1"
                    android:text="✏️ 編輯"
                    android:textColor="#FFFFFF"
                    android:backgroundTint="#1565C0"
                    android:layout_marginEnd="8dp"/>

                <Button
                    android:id="@+id/btnDetailDelete"
                    android:layout_width="0dp"
                    android:layout_height="48dp"
                    android:layout_weight="1"
                    android:text="🗑️ 刪除"
                    android:textColor="#FFFFFF"
                    android:backgroundTint="#C62828"/>
            </LinearLayout>

            <!-- 一覽成績按鈕（有 AI 回覆時才顯示）-->
            <Button
                android:id="@+id/btnViewScore"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:text="📊 一覽成績"
                android:textSize="16sp"
                android:textColor="#FFFFFF"
                android:backgroundTint="#2E7D32"
                android:visibility="gone"/>

        </LinearLayout>
    </ScrollView>
</LinearLayout>
